version: "3.9"
services:
    nginx:
        build:
            context: docker
            dockerfile: dev/nginx/Dockerfile
        ports:
            - "127.0.0.1:8080:80"
        volumes:
            - ./app/public:/app/public
        depends_on:
            - php-fpm
    php-fpm:
        build:
            context: docker
            dockerfile: dev/php-fpm/Dockerfile
        volumes:
            - ./app:/app
    php-cli:
        build:
            context: docker
            dockerfile: dev/php-cli/Dockerfile
        volumes:
            - ./app:/app
    db:
        image: postgres:14
        environment:
#            POSTGRES_USER: postgres
            POSTGRES_PASSWORD: postgres
#            POSTGRES_DB: postgres
        volumes:
            - db:/var/lib/postgresql/data
        ports:
            - "5432:5432"

    adminer:
        image: adminer
        environment:
            ADMINER_DEFAULT_SERVER: db # DB container name
        ports:
            - "8081:8080"

volumes:
    db:
